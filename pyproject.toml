[project]
name = "blog-app"
version = "0.1.0"
description = "Simple Blog app to learn CRUD"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.14",
    "django-filter>=25.2",
    "djangorestframework>=3.17.1",
    "markdown>=3.10.2",
]
